<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Aplikasi Tema -->
    <style name="Theme.Soundmeter" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Tentukan warna-warna utama aplikasi -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/primary_variant_color</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/secondary_color</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>
    </style>

    <!-- Tema gelap -->
    <style name="Theme.Soundmeter.Dark" parent="Theme.Material3.DayNight.DarkActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>
    </style>

    <!-- Gaya Teks untuk Heading -->
    <style name="HeadingText">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

</resources>
